# Core ML pipeline (training + inference)
scikit-learn>=1.4
imbalanced-learn>=0.12
pandas>=2.0
numpy>=1.26
joblib>=1.3
shap>=0.45

# Optional: if installed, XGBoost is fit and compared alongside Logistic
# Regression and Random Forest, and model/train.py will automatically deploy
# whichever of the three scores highest F1 (currently XGBoost, in this repo's
# own training run — see model/model_metadata.json). Without it, training
# still works fine and picks the best of LR / Random Forest.
xgboost>=2.0

# Backend API
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
python-multipart>=0.0.9

# Report generation
reportlab>=4.1
